matplotlib==3.7.5
rerun-sdk==0.20.1
meshcat==0.3.2
sshkeyboard==2.3.1
numpy
opencv-python
pyyaml
tqdm
pyzmq
pytest
# Optional: only speeds up --protocol ws; policy_client falls back to a
# stdlib WebSocket client when this is missing.
# websocket-client
